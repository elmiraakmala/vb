Module koneksi_server
    'biar variabelnya bisa dipanggil di seluruh Module
    ' add new file nya pakai yang modul 
    Public Property Connection1 As String = "server=localhost;user=root;password=;database=elmira;allow user variables=true"
End Module
